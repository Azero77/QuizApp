version: '3.4'

services:
  quizapp.api:
    image: ${DOCKER_REGISTRY-}quizappapi
    build:
      context: .
      dockerfile: QuizApp.API/Dockerfile

  quizapp.bff:
    image: ${DOCKER_REGISTRY-}quizappbff
    build:
      context: .
      dockerfile: QuizApp.BFF/Dockerfile


  quizapp.identity:
    image: ${DOCKER_REGISTRY-}quizappidentity
    build:
      context: .
      dockerfile: QuizApp.Identity/Dockerfile
  quizapp.db.postgres:
    image: postgres:16.1-alpine3.19
    environment:
        POSTGRES_USER: azero
        POSTGRES_PASSWORD: 9b5f1295-7ed2-422f-9b30-6b0b1d513c37
        POSTGRES_DB: QuizAppDatabase
    ports:
        5005:5005
    volumes:
        - ./.containers/db:/var/lib/postgresql/data

  quizapp.db.mongo:
    
